<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp">

    <ImageView
        android:id="@+id/academia_image"
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:scaleType="centerCrop"
        android:padding="10dp"
        android:src="@drawable/academia_image_placeholder" />

    <TextView
        android:id="@+id/academia_name"
        android:fontFamily="sans-serif-condensed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_margin="8dp"
        android:background="#00ACC1"
        android:padding="10dp"
        android:text="Academia Nome"
        android:textColor="@color/black" />

    <Button
        android:id="@+id/view_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:backgroundTint="@color/teal_200"
        android:layout_margin="8dp"
        android:background="@drawable/ic_details"
        android:contentDescription="Ver detalhes" />
</RelativeLayout>