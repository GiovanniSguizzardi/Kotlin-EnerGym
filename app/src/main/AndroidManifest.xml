<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnerGym"
        tools:targetApi="31">

        <!-- Declaração da MainActivity -->
        <activity
            android:name=".activity.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.EnerGym.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat"
            android:exported="true" />

        <!-- Register Activity -->
        <activity
            android:name=".activity.RegisterActivity"
            android:theme="@style/Theme.EnerGym.NoActionBar"
            android:exported="true" />

        <!-- ForgotPassword Activity -->
        <activity
            android:name=".activity.ForgotPasswordActivity"
            android:theme="@style/Theme.EnerGym.NoActionBar"
            android:exported="true" />

        <!-- GymDetails Activity -->
        <activity
            android:name=".activity.AcademiaDetailsActivity"
            android:theme="@style/Theme.EnerGym.NoActionBar"
            android:exported="true" />

        <!-- CadastroAcademia Activity -->
        <activity
            android:name=".activity.CadastrarAcademiaActivity"
            android:theme="@style/Theme.EnerGym.NoActionBar"
            android:exported="true" />

        <!-- EditarAcademia Activity -->
        <activity
            android:name=".activity.EditarAcademiaActivity"
            android:theme="@style/Theme.EnerGym.NoActionBar"
            android:exported="true" />
    </application>
</manifest>